buildscript {
	dependencies {
		classpath files(pluginClasspath.split(','))
	}
}

apply plugin: 'java'
apply plugin: 'io.spring.javaformat'

sourceCompatibility = 1.8

checkFormatMain {
	style = "wide"
}
